# Keep research documentation and source text stable across Windows and Unix.
# Study01/.gitattributes narrows this for the study kit and pins the byte-exact
# Range C comparison targets; keep both files.
* text=auto eol=lf
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
